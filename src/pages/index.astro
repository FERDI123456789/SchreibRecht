---
import Layout from "../layouts/Layout.astro";
import "@/styles/global.css";
import { ArrowRight } from "lucide-react";
---

<Layout
  title="SchreibRecht - Verbessere dein Schreiben und Lesen einfacher denn je!"
>
  <style>
    body {
      overflow: hidden;
    }
    .interactive-svg {
      transition: transform 0.3s ease;
    }
  </style>
  <div class="bg-white">
    <header
      class="relative mt-32 overflow-x-hidden overflow-y-hidden rounded-b-[20%] bg-primary/10 shadow-lg"
    >
      <div class="px-5 md:px-10 relative">
        <div class="mx-auto w-full max-w-7xl">
          <div
            class="mx-auto flex z-10 w-full max-w-4xl flex-col items-center py-12 text-center md:py-16 lg:pb-44 lg:pt-24"
          >
            <h1
              data-aos="fade-up"
              data-aos-duration="900"
              class="mb-4 mt-6 text-5xl font-extrabold uppercase md:text-7xl z-10 text-[#091e3b] aos-init aos-animate"
            >
              Verbessere Dein LRS Mit Spaß
            </h1>
            <p
              data-aos="fade-up"
              data-aos-duration="900"
              class="mb-10 max-w-md z-10 text-[#091e3b] aos-init aos-animate"
            >
              Discover a wealth of valuable no-code tutorialsin our
              comprehensive tutorial hub.
            </p>
            <div class="group flex gap-4 mb-10">
              <a
                href="/auth/signin"
                class="group inline-block uppercase bg-primary hover:bg-white hover:text-primary border border-primary hover:shadow-md hover:shadow-primary focus:shadow-md focus:bg-gray-100 hover:-translate-y-1 focus:-translate-y-1 active:translate-y-0 rounded-md duration-200 px-8 py-4 text-center font-extrabold text-white transition-all"
              >
                Jetzt Kostenlos Starten
                <ArrowRight
                  className="group-hover:-rotate-12 inline mb-0.5"
                  size={20}
                />
              </a>
            </div>
          </div>
        </div>
        <div
          class="lg:w-48 md:w-28 absolute md:bottom-0 right-0 w-16 bottom-32"
        >
          <img
            src="https://assets-global.website-files.com/655eda66dab018e613baf5ff/655f2b28c57aa4c75abf6974_guide-svg4.svg"
            loading="eager"
            width="168"
            alt=""
            class="w-full hover:rotate-3 rounded-full interactive-svg"
          /><img
            src="https://assets-global.website-files.com/655eda66dab018e613baf5ff/655eedbe0ca0432ba7fd096f_guide-svg2.svg"
            loading="eager"
            width="168"
            alt=""
            class="w-full interactive-svg"
          />
        </div>
        <div
          class="md:w-56 w-28 absolute lg:w-96 md:bottom-0 md:top-auto left-0 top-0 bottom-auto"
        >
          <img
            src="https://assets-global.website-files.com/655eda66dab018e613baf5ff/655eedbe09e863ce99b749af_guide-svg3.svg"
            loading="eager"
            alt=""
            class="w-full rounded-full interactive-svg"
          />
          <img
            src="https://assets-global.website-files.com/655eda66dab018e613baf5ff/655f2a8686fbbf271f061f15_guide-hero-svg.svg"
            loading="eager"
            alt=""
            class="w-full is-fixed-width lg:w-72 interactive-svg"
          />
        </div>
      </div>
    </header>
  </div>

  <section class="bg-white">
    <!-- Container -->
    <div class="px-5 py-16 md:px-10 md:py-20">
      <div class="mx-auto w-full max-w-7xl bg-gray-100 px-4 py-32 text-center">
        <!-- Title -->
        <h2
          class="mx-auto mb-6 max-w-3xl flex-col text-3xl font-bold md:mb-10 md:text-5xl lg:mb-12 md:leading-snug"
        >
          Lightning Fast Webflow Dev Made Easy
        </h2>
        <ul
          class="mx-auto mb-8 flex flex-col items-center justify-center gap-5 sm:flex-row sm:gap-8 md:gap-10 lg:mb-12"
        >
          <li class="flex items-center">
            <img
              src="https://assets.website-files.com/6458c625291a94a195e6cf3a/6458c625291a9473e2e6cf65_tick-circle.svg"
              alt=""
              class="mr-2 h-8 w-8"
            />
            <p class="text-sm sm:text-base">300+ UI Blocks</p>
          </li>
          <li class="flex items-center">
            <img
              src="https://assets.website-files.com/6458c625291a94a195e6cf3a/6458c625291a9473e2e6cf65_tick-circle.svg"
              alt=""
              class="mr-2 h-8 w-8"
            />
            <p class="text-sm sm:text-base">Fully responsive</p>
          </li>
          <li class="flex items-center">
            <img
              src="https://assets.website-files.com/6458c625291a94a195e6cf3a/6458c625291a9473e2e6cf65_tick-circle.svg"
              alt=""
              class="mr-2 h-8 w-8"
            />
            <p class="text-sm sm:text-base">Just copy & paste</p>
          </li>
        </ul>
        <a
          href="javascript:void(0);"
          class="mb-4 inline-block items-center bg-black px-6 py-3 text-center font-semibold text-white"
        >
          Get Started
        </a>
        <p class="text-sm sm:text-base">No credit card required.</p>
      </div>
    </div>
  </section>

  <section class="bg-white">
    <!-- Container -->
    <div class="px-5 py-16 md:px-10 md:py-20">
      <div class="mx-auto w-full max-w-7xl bg-gray-100 px-4 py-32 text-center">
        <!-- Title -->
        <h2
          class="mx-auto mb-6 max-w-3xl flex-col text-3xl font-bold md:mb-10 md:text-5xl lg:mb-12 md:leading-snug"
        >
          Lightning Fast Webflow Dev Made Easy
        </h2>
        <ul
          class="mx-auto mb-8 flex flex-col items-center justify-center gap-5 sm:flex-row sm:gap-8 md:gap-10 lg:mb-12"
        >
          <li class="flex items-center">
            <img
              src="https://assets.website-files.com/6458c625291a94a195e6cf3a/6458c625291a9473e2e6cf65_tick-circle.svg"
              alt=""
              class="mr-2 h-8 w-8"
            />
            <p class="text-sm sm:text-base">300+ UI Blocks</p>
          </li>
          <li class="flex items-center">
            <img
              src="https://assets.website-files.com/6458c625291a94a195e6cf3a/6458c625291a9473e2e6cf65_tick-circle.svg"
              alt=""
              class="mr-2 h-8 w-8"
            />
            <p class="text-sm sm:text-base">Fully responsive</p>
          </li>
          <li class="flex items-center">
            <img
              src="https://assets.website-files.com/6458c625291a94a195e6cf3a/6458c625291a9473e2e6cf65_tick-circle.svg"
              alt=""
              class="mr-2 h-8 w-8"
            />
            <p class="text-sm sm:text-base">Just copy & paste</p>
          </li>
        </ul>
        <a
          href="javascript:void(0);"
          class="mb-4 inline-block items-center bg-black px-6 py-3 text-center font-semibold text-white"
        >
          Get Started
        </a>
        <p class="text-sm sm:text-base">No credit card required.</p>
      </div>
    </div>
  </section>

  <script>
    document.addEventListener("DOMContentLoaded", () => {
      const svgs = document.querySelectorAll(".interactive-svg");
      const maxDistance = 300; // Maximum distance for movement effect
      const maxRotation = 30; // Maximum rotation in degrees
      const sensitivity = 0.1; // Sensitivity for movement

      document.addEventListener("mousemove", (e) => {
        const centerX = window.innerWidth / 2;
        const centerY = window.innerHeight / 2;
        const mouseX = e.clientX;
        const mouseY = e.clientY;

        svgs.forEach((svg) => {
          const rect = svg.getBoundingClientRect();
          const svgX = rect.left + rect.width / 2;
          const svgY = rect.top + rect.height / 2;

          // Calculate vector from SVG to cursor
          const dx = mouseX - svgX;
          const dy = mouseY - svgY;
          const distance = Math.sqrt(dx * dx + dy * dy);

          // Calculate rotation to face cursor
          const angle = Math.atan2(dy, dx) * (180 / Math.PI);
          const normalizedAngle = ((angle + 180) % 360) - 180;

          // Calculate movement away from center based on cursor position
          const centerDx = mouseX - centerX;
          const centerDy = mouseY - centerY;
          const centerDistance = Math.sqrt(
            centerDx * centerDx + centerDy * centerDy
          );
          const moveX =
            (centerDx / centerDistance || 0) *
            Math.min(centerDistance, maxDistance) *
            sensitivity;
          const moveY =
            (centerDy / centerDistance || 0) *
            Math.min(centerDistance, maxDistance) *
            sensitivity;

          // Apply rotation and translation with type assertion
          (svg as HTMLElement).style.transform =
            `rotate(${normalizedAngle * 0.5}deg) translate(${-moveX}px, ${-moveY}px)`;
        });
      });

      // Reset position and rotation when mouse leaves
      document.addEventListener("mouseleave", () => {
        svgs.forEach((svg) => {
          (svg as HTMLElement).style.transform =
            "rotate(0deg) translate(0px, 0px)";
        });
      });
    });
  </script>
</Layout>
